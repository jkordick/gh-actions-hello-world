name: Post welcome comment
on:
  pull_request:
    types: [opened]
jobs:
  build:
    name: Post welcome comment
    runs-on: ubuntu-latest
    steps:
      - name: powershell echo
        shell: pwsh
        run: |
          Write-Host "🎉 The job was automatically triggered by a ${{ github.event_name }} event."

